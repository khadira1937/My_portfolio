name: my-portfolio
services:
  - name: web
    github:
      repo: khadira1937/My_portfolio
      branch: master
    build_command: npm run build
    output_dir: dist
    environment_slug: node-js
    routes:
      - path: /
